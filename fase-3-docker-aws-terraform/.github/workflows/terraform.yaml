name: Terraform CI/CD

on:
  workflow_dispatch:
    inputs:
      apply:
        description: 'Executar o Terraform apply'
        required: true
        default: 'false'
        type: choice
        options:
          - true
          - false

permissions:
  id-token: write
  contents: read

jobs: 
  job1: 
    name: Terraform
    runs-on: ubuntu-latest 

    steps: 
      - name: Checkout
        uses: actions/checkout@v5

      - name: Configure AWS Credentials 
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: arn:aws:iam::339855175962:role/GitHubinfraRole
          aws-region: us-east-2

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v4

        name: Terraform Init
        run: cd fase-3-docker-aws-terraform && terraform init

      - name: Terraform Validate
        run: cd fase-3-docker-aws-terraform && terraform validate

      - name: Terraform Plan
        run: cd fase-3-docker-aws-terraform && terraform plan -out=tfplan

      - name: Terraform Apply
        run: cd fase-3-docker-aws-terraform && terraform apply -auto-approve tfplan
        if: github.event.inputs.apply == 'true'

